# OpenAI API 키 (임베딩 text-embedding-3-small + 챗 gpt-4o-mini). 서버에서만 사용.
OPENAI_API_KEY=

# "1"이면 항상 data/*.mock.json 사용. A의 실데이터(articles.json/embeddings.json)가
# 도착하면 이 값을 비우면 됨(코드 변경 0). 비어있으면 실데이터 우선, 없으면 mock 폴백.
NEWSLENS_USE_MOCK=1
